# This property tells the Android plugin to use the AndroidX libraries,
# which is required for all modern Android projects.
android.useAndroidX=true

# This property tells the Android plugin to automatically migrate
# any third-party libraries to use AndroidX.
android.enableJetifier=true
